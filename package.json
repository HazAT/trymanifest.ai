{
  "name": "trymanifest.ai",
  "version": "0.1.0",
  "description": "Production is our dev environment.",
  "type": "module",
  "scripts": {
    "dev": "bun --hot index.ts",
    "start": "bun index.ts",
    "test": "bun test",
    "manifest": "bun manifest/cli/index.ts",
    "build:site": "bun frontend/site/build.ts",
    "build": "bun frontend/site/build.ts && bun manifest/cli/index.ts frontend build"
  },
  "dependencies": {
    "@mariozechner/pi-coding-agent": "0.52.10",
    "tailwindcss": "^4.1.18"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
